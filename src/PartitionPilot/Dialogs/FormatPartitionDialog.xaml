<Window x:Class="PartitionPilot.Dialogs.FormatPartitionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Format Partition" Width="440" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Style="{StaticResource DialogWindow}">
    <Border Style="{StaticResource Card}" Margin="16">
        <StackPanel>
            <TextBlock Text="Format Partition" Style="{StaticResource SectionTitleText}" Margin="0,0,0,6"/>
            <Border Style="{StaticResource DangerNoticePanel}">
                <TextBlock Text="Formatting replaces the file system on this volume and removes its existing files."
                           Style="{StaticResource CaptionText}"
                           Foreground="{DynamicResource DangerBtnFgBrush}"/>
            </Border>

            <TextBlock Text="Current volume" Style="{StaticResource FieldLabelText}" Margin="0,0,0,4"/>
            <TextBlock x:Name="txtInfo" Foreground="{DynamicResource PrimaryBrush}" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,12"/>

            <TextBlock Text="File system" Style="{StaticResource FieldLabelText}" Margin="0,0,0,4"/>
            <ComboBox x:Name="cmbFS" Height="34" FontSize="13" Margin="0,0,0,10"
                      AutomationProperties.Name="File system">
                <ComboBoxItem Content="NTFS" IsSelected="True"/>
                <ComboBoxItem Content="FAT32"/>
                <ComboBoxItem Content="exFAT"/>
                <ComboBoxItem Content="ReFS"/>
            </ComboBox>

            <TextBlock Text="Volume label" Style="{StaticResource FieldLabelText}" Margin="0,0,0,4"/>
            <TextBox x:Name="txtLabel" Height="34" FontSize="13" Margin="0,0,0,10"
                     AutomationProperties.Name="Volume label"/>

            <CheckBox x:Name="chkQuick" Content="Quick format" IsChecked="True" Margin="0,0,0,16"/>

            <Border Style="{StaticResource DialogFooter}">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Format" Width="96" Height="34" Style="{StaticResource DangerButton}"
                            IsDefault="True" Click="OnOk"/>
                    <Button Content="Cancel" Width="96" Height="34" Margin="8,0,0,0" IsCancel="True"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Border>
</Window>
