<Window x:Class="PartitionPilot.Dialogs.SplitPartitionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:PartitionPilot"
        Title="{local:Loc Xaml_SplitPartition}" Width="440" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Style="{StaticResource DialogWindow}">
    <Border Style="{StaticResource Card}" Margin="16">
        <StackPanel>
            <TextBlock Text="{local:Loc Xaml_SplitPartition}" Style="{StaticResource SectionTitleText}" Margin="0,0,0,6"/>
            <TextBlock x:Name="txtInfo" Style="{StaticResource CaptionText}" Foreground="{DynamicResource PrimaryBrush}" Margin="0,0,0,14"/>

            <TextBlock Text="{local:Loc Xaml_NewPartitionSizeGB}" Style="{StaticResource FieldLabelText}" Margin="0,0,0,4"/>
            <TextBox x:Name="txtNewSize" Height="34" FontSize="13" Margin="0,0,0,10"
                     AutomationProperties.Name="{local:Loc Xaml_NewPartitionSizeInGB}"/>

            <TextBlock Text="{local:Loc Xaml_NewDriveLetter}" Style="{StaticResource FieldLabelText}" Margin="0,0,0,4"/>
            <ComboBox x:Name="cmbLetter" Height="34" FontSize="13" Margin="0,0,0,10"
                      AutomationProperties.Name="{local:Loc Xaml_NewDriveLetter}"/>

            <TextBlock Text="{local:Loc Xaml_FileSystem}" Style="{StaticResource FieldLabelText}" Margin="0,0,0,4"/>
            <ComboBox x:Name="cmbFS" Height="34" FontSize="13" Margin="0,0,0,10"
                      AutomationProperties.Name="{local:Loc Xaml_FileSystem}">
                <ComboBoxItem Content="{local:Loc Xaml_NTFS}" Tag="NTFS" IsSelected="True"/>
                <ComboBoxItem Content="{local:Loc Xaml_FAT32}" Tag="FAT32"/>
                <ComboBoxItem Content="{local:Loc Xaml_ExFAT}" Tag="exFAT"/>
                <ComboBoxItem Content="{local:Loc Xaml_ReFS}" Tag="ReFS"/>
            </ComboBox>

            <TextBlock Text="{local:Loc Xaml_VolumeLabel}" Style="{StaticResource FieldLabelText}" Margin="0,0,0,4"/>
            <TextBox x:Name="txtLabel" Height="34" FontSize="13" Margin="0,0,0,16"
                     AutomationProperties.Name="{local:Loc Xaml_VolumeLabel}"/>

            <Border Style="{StaticResource DialogFooter}">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="{local:Loc Split}" Width="96" Height="34" Style="{StaticResource PrimaryButton}"
                            IsDefault="True" Click="OnOk"/>
                    <Button Content="{local:Loc Cancel}" Width="96" Height="34" Margin="8,0,0,0" IsCancel="True"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Border>
</Window>
