<Window x:Class="PartitionPilot.Dialogs.PasswordPromptDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:PartitionPilot"
        Title="{local:Loc Password}"
        Width="440"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Style="{StaticResource DialogWindow}">
    <Border Style="{StaticResource Card}" Margin="16">
        <StackPanel>
            <TextBlock x:Name="PromptText"
                       Style="{StaticResource CaptionText}"
                       Margin="0,0,0,12"/>

            <TextBlock Text="{local:Loc Password}"
                       Style="{StaticResource FieldLabelText}"
                       Margin="0,0,0,4"/>
            <PasswordBox x:Name="PasswordInput"
                         Height="34"
                         Padding="8,5"
                         Margin="0,0,0,16"
                         Foreground="{DynamicResource TextBrush}"
                         Background="{DynamicResource InputBgBrush}"
                         BorderBrush="{DynamicResource BorderBrush}"
                         BorderThickness="1"
                         AutomationProperties.Name="{local:Loc Password}"/>

            <Border Style="{StaticResource DialogFooter}">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="{local:Loc OK}"
                            Width="96"
                            Height="34"
                            IsDefault="True"
                            Style="{StaticResource PrimaryButton}"
                            Click="OnConfirm"/>
                    <Button Content="{local:Loc Cancel}"
                            Width="96"
                            Height="34"
                            Margin="8,0,0,0"
                            IsCancel="True"
                            Style="{StaticResource SecondaryButton}"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Border>
</Window>
