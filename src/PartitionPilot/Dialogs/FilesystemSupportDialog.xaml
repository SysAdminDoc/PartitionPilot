<Window x:Class="PartitionPilot.Dialogs.FilesystemSupportDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Filesystem Support Matrix" Width="700" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Style="{StaticResource DialogWindow}">
    <Border Style="{StaticResource Card}" Margin="16">
        <StackPanel>
            <TextBlock Text="Filesystem Support" Style="{StaticResource SectionTitleText}" Margin="0,0,0,6"/>
            <TextBlock Text="Operations supported by PartitionPilot per filesystem type. Limitations are imposed by Windows Storage APIs."
                       Style="{StaticResource CaptionText}" TextWrapping="Wrap" Margin="0,0,0,14"/>

            <DataGrid x:Name="matrixGrid" AutoGenerateColumns="False" IsReadOnly="True"
                      HeadersVisibility="Column" GridLinesVisibility="All"
                      BorderThickness="1" BorderBrush="{DynamicResource BorderBrush}"
                      Background="{DynamicResource CardBrush}"
                      RowBackground="{DynamicResource CardBrush}"
                      AlternatingRowBackground="{DynamicResource SurfaceBrush}"
                      CanUserSortColumns="False" CanUserResizeRows="False"
                      CanUserReorderColumns="False"
                      AutomationProperties.Name="Filesystem support matrix">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Filesystem" Binding="{Binding FileSystem}" Width="90" FontWeight="SemiBold"/>
                    <DataGridTextColumn Header="Create" Binding="{Binding Create}" Width="70"/>
                    <DataGridTextColumn Header="Format" Binding="{Binding Format}" Width="70"/>
                    <DataGridTextColumn Header="Resize" Binding="{Binding Resize}" Width="70"/>
                    <DataGridTextColumn Header="Extend" Binding="{Binding Extend}" Width="70"/>
                    <DataGridTextColumn Header="Check" Binding="{Binding Check}" Width="70"/>
                    <DataGridTextColumn Header="Label" Binding="{Binding Label}" Width="70"/>
                    <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>

            <Border Style="{StaticResource DialogFooter}" Margin="0,14,0,0">
                <Button Content="Close" Width="96" Height="34" HorizontalAlignment="Right" IsCancel="True"/>
            </Border>
        </StackPanel>
    </Border>
</Window>
